=== PRIORIDAD 1 — BUILD ERRORS (blocker) ===

Hay errores en los archivos del último commit (google pay demo).
Corregir antes de continuar:

VioProductDetailOverlay.kt
    Unresolved reference 'VioPaymentMethod' → importar live.vio.VioCore.models.VioPaymentMethod
    Unresolved reference 'variant'
    Unresolved reference 'times' (operator *)
    Unresolved reference 'gateway', 'gatewayMerchantId', 'currency'

PaymentManager.kt / PaymentRepository.kt / PaymentRepositoryGraphQL.kt
    Unresolved reference 'InitGooglePayDto' / 'ConfirmGooglePayDto'
    → Asegurarse de que PaymentModels.kt está en el classpath correcto
    → Revisar que los DTOs tienen los campos: status, gateway, gatewayMerchantId, currency

Verificar build: ./gradlew :library:assembleDebug

---

=== PRIORIDAD 2 — ZERO-CONFIG SDK (nuevo modelo) ===

CONTEXTO:
El SDK ya NO requiere vio-config.json para funcionar.
El cliente solo necesita su API key — todo lo demás viene del backend.

El backend tiene un nuevo endpoint (live hoy):
  GET https://api-dev.vio.live/v1/sdk/config?apiKey=<apiKey>
  
Respuesta:
{
  "clientApp": { "id": 17, "name": "Viaplay", "apiKey": "..." },
  "endpoints": {
    "restBase": "https://api-dev.vio.live",
    "webSocketBase": "https://api-dev.vio.live",
    "commerceGraphQL": "https://graph-ql-dev.vio.live/graphql"
  },
  "features": { "engagement": true, "adPlacements": true, "commerce": true, "lineup": true },
  "commerce": { "apiKey": "KCXF10Y-W5T4PCR-GG5119A-Z64SQ9S", "endpoint": "https://graph-ql-dev.vio.live/graphql" },
  "theme": { "primaryColor": null, "accentColor": null },
  "markets": []
}

En Swift ya implementamos:
  VioSDK.configure(apiKey: "xxx")        // todo lo demás viene del backend
  VioSDK.setContent(id: stream.contentId) // cuando el usuario abre un stream

LO QUE SE ESPERA EN KOTLIN:

a) Crear VioRemoteConfig.kt — modelo Kotlin para la response anterior:
   data class VioRemoteConfig(
     val clientApp: RemoteClientApp?,
     val endpoints: RemoteEndpoints?,
     val features: RemoteFeatures?,
     val commerce: RemoteCommerceConfig?,
     val theme: RemoteThemeConfig?,
     val markets: List<String>?
   )
   (Con sus data classes anidadas)

b) Crear VioSDKConfigService.kt — fetches GET /v1/sdk/config:
   class VioSDKConfigService {
     suspend fun fetchConfig(apiKey: String, baseUrl: String): VioRemoteConfig?
   }
   - Usar el HttpClient/Retrofit existente
   - Fallback graceful: si falla, retornar null (SDK sigue con defaults)
   - Log de warning si falla — no crashear

c) VioSDK.kt (nuevo entry point público) o actualizar VioSDK.configure():
   object VioSDK {
     fun configure(apiKey: String, environment: VioEnvironment = VioEnvironment.PRODUCTION)
     fun setContent(contentId: String)
     fun clearContent()
   }
   
   configure() debe:
   1. Guardar apiKey en VioConfiguration
   2. Aplicar defaults inmediatamente (SDK usable antes del fetch remoto)
   3. Lanzar coroutine para fetchConfig() → cuando llega, aplicar endpoints/commerce

d) VioConfiguration.applyRemoteConfig(config: VioRemoteConfig):
   - Actualizar campaignConfiguration.restAPIBaseURL y webSocketBaseURL
   - Actualizar commerceApiKey (nuevo campo en config)
   - Llamar CampaignManager.reinitialize() después

IMPORTANTE: vio-config.json puede seguir funcionando para compatibilidad,
pero configure(apiKey:) debe funcionar SIN él.

---

=== PRIORIDAD 3 — discoverCampaigns fix ===

(ya hay tarjeta Trello con detalle)
Cambiar prioridad de api key en discoverCampaigns():
  // Actual (incorrecto):
  val effectiveApiKey = campaignAdminApiKey?.takeIf { it.isNotBlank() } ?: apiKey
  
  // Correcto:
  val effectiveApiKey = when {
    !campaignApiKey.isNullOrBlank() -> campaignApiKey
    !campaignAdminApiKey.isNullOrBlank() -> campaignAdminApiKey
    else -> apiKey
  }

Añadir broadcastId param:
  val url = "$baseUrl/v1/sdk/campaigns?apiKey=$effectiveApiKey" +
    if (matchId != null) "&broadcastId=$matchId&matchId=$matchId" else ""

---

=== PRIORIDAD 4 — Sponsor Moments (nuevo tipo en API) ===

El backend ahora devuelve un campo "type" en los sponsor slots.
Tipos: product, lead, poll_cta, contest_cta, link
Cada tipo tiene su propio "config" JSON.

LO QUE SE ESPERA EN KOTLIN:
a) Añadir SponsorMoment.type: String a los modelos existentes
b) Añadir SponsorMoment.config: Map<String, Any?> (o JsonObject) 
c) La UI (SponsorMomentCard o equivalente) debe renderizar diferente según type:
   - product → mostrar producto + precio + CTA compra
   - lead → form de captura (email/phone)
   - poll_cta → botón "Stem nå" que abre el poll
   - contest_cta → botón "Delta" que abre el contest
   - link → botón que abre URL externa

Por ahora alcanza con manejar "product" (que es el default y el 100% de los slots actuales).
Los otros tipos pueden quedar como fallback genérico hasta que haya diseño.
