<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.vio.viaplaydemo">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:label="@string/app_name"
        android:theme="@style/Theme.TV2DemoApp"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:largeHeap="true"
        android:hardwareAccelerated="true">
        <activity
            android:name="com.vio.viaplaydemo.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep links for web-based payment providers (Klarna/Vipps) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vio-demo" android:host="checkout"
                    android:pathPrefix="/success" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="vio-demo" android:host="checkout"
                    android:pathPrefix="/cancel" />
            </intent-filter>
        </activity>
        <activity
            android:name="live.vio.VioUI.KlarnaNativeActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@style/Theme.TV2DemoApp"
            android:hardwareAccelerated="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <!-- Klarna native payments - no intent filter needed for HTTPS return URL -->
        </activity>
        <activity
            android:name="live.vio.VioUI.KlarnaWebActivity"
            android:exported="false"
            android:theme="@style/Theme.TV2DemoApp"
            tools:replace="android:theme" />
    </application>
</manifest>
