package live.vio.VioTesting

/**
 * Kotlin counterpart to the Swift `VioTesting` entry point.
 */
object VioTesting {
    fun configure() {
        println("🧪 Vio Testing utilities initialized")
    }
}

typealias VioMockDataProvider = MockDataProvider
